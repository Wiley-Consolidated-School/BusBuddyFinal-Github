using System;

namespace BusBuddy.Models
{
    // This file contains partial class definitions for models
    // These were extracted to avoid duplicate type definitions
    // Only used for support types that aren't already defined elsewhere
    // Note: The primary models (Vehicle, Route, TimeCard, Driver, Activity)
    // are defined in their own files
}

